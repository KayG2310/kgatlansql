import React from "react";

function RunButton({ handleRunQuery }) {
  <button onClick={() => { console.log("Button clicked!"); executeQuery(); }}>
  Execute Query
</button>

}

export default RunButton;
